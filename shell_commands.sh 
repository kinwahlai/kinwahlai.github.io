#!/bin/sh

bundle install

npm install

bundle exec jekyll serve --config localhost_config.yml

bundle exec octopress new post "Post title" --dir blog

bundle exec octopress new draft "Post title"

bundle exec octopress publish "Post title" --dir blog
